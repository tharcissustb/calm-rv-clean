dataset:
  name: pheme
  path: data/pheme.jsonl
  text_field: text
  label_field: label

model:
  num_labels: 3
  max_length: 128

training:
  batch_size: 32
  lr: 0.001
  epochs: 10
  seed: 42

output:
  dir: outputs/runs