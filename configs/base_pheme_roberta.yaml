dataset:
  name: pheme
  path: data/pheme.jsonl
  text_field: text
  label_field: label

model:
  name: roberta-base
  num_labels: 3
  max_length: 256

training:
  batch_size: 16
  lr: 0.00002
  epochs: 5
  seed: 42

output:
  dir: outputs/runs